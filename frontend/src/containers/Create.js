import React from 'react';
import CreatePost from '../components/CreatePost';

const Create = () => {
    return (
        <div>
            <CreatePost />
        </div>
    );
};

export default Create;
